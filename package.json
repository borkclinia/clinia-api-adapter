{
  "name": "clinia-api-adapter",
  "version": "1.0.0",
  "description": "API adapter seguindo o padrão Clinia.io que busca dados da API Clinica Salute",
  "main": "dist/server.js",
  "engines": {
    "node": ">=18.0.0",
    "bun": ">=1.0.0"
  },
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "bun build src/server.ts --outdir ./dist --target node",
    "start": "node dist/server.js",
    "start:bun": "bun run dist/server.js",
    "lint": "tsc --noEmit",
    "test": "echo \"Error: no test specified\" && exit 1",
    "render-build": "bun install && bun run build",
    "render-start": "bun start"
  },
  "keywords": ["api", "adapter", "clinia", "clinica-salute", "healthcare", "typescript", "bun"],
  "author": "borkclinia",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.11.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/morgan": "^1.9.10",
    "@types/node": "^24.1.0",
    "typescript": "^5.9.2"
  }
}
